import {observer} from "mobx-react";
import {TextareaAutosize} from "@mui/material";

interface Props{

}

const RegisterBoardContainer = observer(({

}:Props)=>{

    return(
        <>
            <TextareaAutosize/>
        </>
    )
})

export default RegisterBoardContainer;
